# Secrets that have to be added for your environment

C-string defines that have to be added to the `secrets.h` file in the `src` folder:
- WIFI_SSID
- WIFI_PASSWORD
- ACCESS_USERNAME
- ACCESS_PASSWORD